// Placeholder entry point — see ../README.md. Not yet wired to a UI
// framework or to the backend/ pipeline.

document.addEventListener("DOMContentLoaded", () => {
  const root = document.getElementById("root");
  if (root) {
    root.textContent = "Anime-Stitch-Pipeline frontend scaffold — not yet implemented.";
  }
});
