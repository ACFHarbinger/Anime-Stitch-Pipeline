# tools/build — compile every module.

set shell := ["bash", "-c"]

# Build all modules
all: backend base

backend:
    cd backend && uv build

base:
    cmake -S base -B base/build -DCMAKE_BUILD_TYPE=Release && cmake --build base/build -j
